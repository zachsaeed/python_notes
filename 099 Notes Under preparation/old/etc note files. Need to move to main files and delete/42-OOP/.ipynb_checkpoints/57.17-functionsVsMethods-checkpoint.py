# Function:
# When we have a function floating on its own, like print(), len(), range()

# Method
# A function which is called via an object like string_var.upper(), astring_var.append()
# are all string methods
